name: tests

on:
  push:
  pull_request:

jobs:
  pytest:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install deps
        run: |
          python -m pip install -U pip
          pip install -e .
          # dépendances de test
          pip install pytest pytest-cov jsonschema

      - name: Run pytest with coverage
        run: |
          pytest -q --cov=digitalmeve --cov-report=xml --cov-report=term

      - name: Upload coverage.xml artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.python-version }}
          path: coverage.xml

      # Optionnel : décommente si tu veux envoyer à Codecov (public repo = pas de token)
      # - name: Upload to Codecov
      #   if: ${{ matrix.python-version == '3.11' }}  # un seul upload suffit
      #   uses: codecov/codecov-action@v4
      #   with:
      #     files: coverage.xml
      #     fail_ci_if_error: true
